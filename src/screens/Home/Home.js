import React from 'react';
import {View, Text} from 'react-native';
import {screens, delivery_time, ratings, tags} from '../../constants';

const Home = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
